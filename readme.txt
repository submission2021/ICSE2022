SubtractionSummaryData.csv contaisn the data generated by our study, 
stating the fault detection and deterministic fault detection per mutant subject we created.
We also looked at which test suite types found faults (Specification vs Invafriant)

Subtraction-Specification-100/ contains the output from the Matlab program when evaluating the specification test suite. 


Subtraction/ contains the files need to run ChemFlow on a subject, in this case Subtraction.