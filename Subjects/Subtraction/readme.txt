This directory is the full set of text implementations of the golden model (S0, S100) and mutants.
Subjects 100-110 are from Gerten et. al

MutationTable-S.csv contains all the reactions for each mutant in a table, so that the differences can be more easily seen